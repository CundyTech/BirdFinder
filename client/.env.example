# Copy this file to .env and fill in real values. .env is gitignored;
# this example file is the template that stays committed.

# Must match the API_KEY the server was started with (see api/README.md).
EXPO_PUBLIC_API_KEY=REPLACE_WITH_YOUR_API_KEY
