# Note: TensorFlow binary wheels may be unavailable for some Python/OS/architecture combinations
# (e.g. Windows ARM64 or very new Python versions). See the repository README for guidance.
# This environment is for training only (e.g. the Windows VM) — the ARM64
# inference host uses onnxruntime instead, see model/build/requirements-inference.txt.
tensorflow==2.21.0
scipy==1.17.1
Pillow==12.1.1
numpy==2.4.3
tf2onnx
